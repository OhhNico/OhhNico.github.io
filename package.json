{
  "name": "ohhnico-github-io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site for Nicolo Rengifo. Tailwind source in src/input.css compiles to style.css, which is committed and is the only stylesheet the page loads. GSAP is vendored into vendor/ rather than fetched, so the published page makes no third-party requests.",
  "scripts": {
    "build": "tailwindcss --input src/input.css --output style.css --minify",
    "dev": "tailwindcss --input src/input.css --output style.css --watch",
    "vendor": "cp node_modules/gsap/dist/gsap.min.js node_modules/three/build/three.module.min.js node_modules/three/build/three.core.min.js vendor/",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "gsap": "3.15.0",
    "tailwindcss": "4.3.3",
    "three": "0.185.1"
  }
}
