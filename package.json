{
  "name": "ohhnico-github-io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site for Nicolo Rengifo. Tailwind source in src/input.css compiles to style.css, which is committed and is the only stylesheet the page loads.",
  "scripts": {
    "build": "tailwindcss --input src/input.css --output style.css --minify",
    "dev": "tailwindcss --input src/input.css --output style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
